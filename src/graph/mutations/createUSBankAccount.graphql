#import "../queries/payableAccountFragment.graphql"

mutation CreateUSBankAccount($createUSAccountInput: USBankAccountInput!) {
    createUSBankAccount(createUSAccountInput: $createUSAccountInput) {
        ...PayableAccountFragment
    }
}
