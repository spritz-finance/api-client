#import "../queries/payableAccountFragment.graphql"

mutation RenamePayableAccount($accountId: String!, $name: String!) {
    renamePayableAccount(accountId: $accountId, name: $name) {
        ...PayableAccountFragment
    }
}
