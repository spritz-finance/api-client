fragment VerificationFragment on Verification {
    userId
    identity {
        canRetry
        status
        country
        verificationUrl
        verificationMetadata {
            failureReason
            details {
                matchedEmail
            }
        }
    }
}
