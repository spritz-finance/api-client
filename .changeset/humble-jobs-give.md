---
'@spritz-finance/api-client': patch
---

Make `sandbox` the canonical environment value while retaining `staging` as a deprecated input alias, and document the Developer Access credential boundaries.
