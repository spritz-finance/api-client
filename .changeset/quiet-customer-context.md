---
'@spritz-finance/api-client': patch
---

Type the optional `customerContext` object accepted by `client.deposit.prepare(...)` and `client.sandbox.prepareDepositWithProgramControl(...)`, regenerated from the sandbox contract.
